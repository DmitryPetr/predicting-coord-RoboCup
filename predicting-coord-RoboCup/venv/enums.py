from enum import Enum

class ACTION_PLAYER(Enum):
    BALL_FIGHT = 'fight' # fighting for the ball
    DRIBLING = 'dribbling' # dribbling
    PASSING = 'passing' # passing
    KICK_GOAL = 'kickingG' # kicking the goal
    SEARCHING = 'searching' # kicking the goal